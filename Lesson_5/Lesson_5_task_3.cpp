#include <iostream>

using namespace std;

//task_3
void PrintArray(int arr[], int size)
{
	for (size_t a = 1; a <=22; a += 3)
	{
		cout << arr[a] << " " ;
	}
}

int main()
{
	const int mysize = 8;
	int myarr[1] = { 1 };
	for (myarr[1])

	PrintArray(myarr, mysize);
	return 0;
}